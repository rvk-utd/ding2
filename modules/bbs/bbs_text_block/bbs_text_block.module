<?php

/**
 * @file
 * BBS text Block module main file.
 */

/**
 * Implements hook_ctools_plugin_directory().
 */
function bbs_text_block_ctools_plugin_directory($module, $plugin) {
    if ($module == 'ctools' || $module == 'panels') {
        return 'plugins/' . $plugin;
    }
}

